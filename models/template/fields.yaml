# ===================================
#  Form Field Definitions
# ===================================

fields:
    title:
        label: dubk0ff.unicrumbs::models.template.title
        type: text
        placeholder: dubk0ff.unicrumbs::models.template.title_placeholder

    is_active:
        label: dubk0ff.unicrumbs::models.template.is_active
        type: switch
        default: true

    code:
        label: dubk0ff.unicrumbs::models.template.code
        type: codeeditor
        size: giant
        language: twig
