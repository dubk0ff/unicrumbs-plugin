# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: dubk0ff.unicrumbs::models.crumb.name
        type: text
        searchable: true

    id:
        label: dubk0ff.unicrumbs::models.crumb.id
        searchable: false

    parent:
        label: dubk0ff.unicrumbs::models.crumb.parent
        relation: parent
        select: name
        searchable: false

    type_info:
        label: dubk0ff.unicrumbs::models.crumb.type
        type: text
        searchable: false

    deleted_at:
        label: dubk0ff.unicrumbs::models.deleted_at
        default: —

    created_at:
        label: dubk0ff.unicrumbs::models.created_at
        invisible: true

    updated_at:
        label: dubk0ff.unicrumbs::models.updated_at
